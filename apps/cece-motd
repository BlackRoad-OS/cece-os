#!/bin/bash
# CECE-MOTD v1.0 - Message of the Day
show() {
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "  🖤 Welcome to CECE OS"
    echo "  📅 $(date '+%A, %B %d, %Y')"
    echo "  🕐 $(date '+%H:%M:%S')"
    echo "  💻 $(hostname) | $(whoami)"
    echo "  📊 Apps: $(ls ~/cece/apps 2>/dev/null | wc -l)"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
}
case "$1" in *) show ;; esac
