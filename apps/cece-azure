#!/bin/bash
# CECE-AZURE v1.0 - Azure CLI Helper
groups() { az group list; }
vms() { az vm list; }
storage() { az storage account list; }
case "$1" in
    groups) groups ;;
    vms) vms ;;
    storage) storage ;;
    *) echo "CECE-AZURE: groups | vms | storage" ;;
esac
