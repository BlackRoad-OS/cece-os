#!/bin/bash
# CECE-EMOJI v1.0 - Emoji Library
search() {
    QUERY="$1"
    case "$QUERY" in
        *happy*|*smile*) echo "😀 😃 😄 😁 😊 🙂" ;;
        *sad*) echo "😢 😭 😞 😔 🙁" ;;
        *love*|*heart*) echo "❤️ 💕 💖 💗 😍 🥰" ;;
        *fire*) echo "🔥 🔥 🔥" ;;
        *star*) echo "⭐ 🌟 ✨ 💫" ;;
        *music*) echo "🎵 🎶 🎤 🎸 🎹" ;;
        *food*) echo "🍕 🍔 🌮 🍣 🍜" ;;
        *animal*) echo "🐶 🐱 🐼 🦊 🐸" ;;
        *) echo "🖤 BlackRoad emoji: 🛣️ 🖤 ⚫ 🌑" ;;
    esac
}
random() {
    EMOJIS=("🎉" "🚀" "💫" "🔥" "⭐" "💎" "🌈" "🎯" "💪" "🏆")
    echo "${EMOJIS[$RANDOM % ${#EMOJIS[@]}]}"
}
case "$1" in
    search) search "$2" ;;
    random) random ;;
    *) echo "CECE-EMOJI: search <word> | random" ;;
esac
