#!/bin/bash
# Serve CECE OS dashboard
# Usage: cece-serve [port]

PORT=${1:-8080}
echo "💜 CECE OS Dashboard starting on http://localhost:$PORT"
echo "   Press Ctrl+C to stop"
cd ~/cece/os/ui
python3 -m http.server $PORT
